<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Jammer</title>
    <!-- Modern UI CSS -->
    <link rel="stylesheet" href="/css/modern-style.css" />
    <link rel="stylesheet" href="/dist/output-tailwind.css" />
    <!-- Icon Set -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body class="bg-surface min-h-screen flex flex-col">
    <!-- Simple Header -->
    <header class="bg-white py-4 shadow-soft">
        <div class="container mx-auto px-4">
            <div class="flex items-center justify-between">
                <!-- Logo -->
                <a href="/" class="text-2xl font-bold text-primary">Jammer</a>
                
                <!-- Navigation Links -->
                <nav class="hidden md:flex items-center space-x-6">
                    <a href="/" class="nav-link py-2">Home</a>
                    <a href="/Home/About" class="nav-link py-2">About</a>
                    <a href="/Home/Contact" class="nav-link py-2">Contact</a>
                </nav>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex-grow flex items-center justify-center py-12">
        <div class="container mx-auto px-4">
            @RenderBody()
        </div>
    </main>

    <!-- Simple Footer -->
    <footer class="bg-secondary-900 text-white py-6">
        <div class="container mx-auto px-4">
            <div class="flex flex-col md:flex-row justify-between items-center">
                <div class="mb-4 md:mb-0">
                    <p class="text-secondary-300">&copy; @DateTime.Now.Year Jammer. All rights reserved.</p>
                </div>
                <div class="flex space-x-4">
                    <a href="#" class="text-secondary-300 hover:text-white transition-colors">
                        <i class="bi bi-facebook text-xl"></i>
                    </a>
                    <a href="#" class="text-secondary-300 hover:text-white transition-colors">
                        <i class="bi bi-instagram text-xl"></i>
                    </a>
                    <a href="#" class="text-secondary-300 hover:text-white transition-colors">
                        <i class="bi bi-twitter-x text-xl"></i>
                    </a>
                    <a href="#" class="text-secondary-300 hover:text-white transition-colors">
                        <i class="bi bi-youtube text-xl"></i>
                    </a>
                </div>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="/assets/js/jquery.min.js"></script>
    
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
